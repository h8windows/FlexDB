<%= form_for @market do |f| %>
  <%= f.error_messages %>
  <div class="field">
	<%= f.label :name %>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit "Create Market" %>
  </div>
<% end %>