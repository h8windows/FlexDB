<%= form_for @market do |f| %>
  <!%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
	<%= f.label :name %>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit "Create Market" %>
  </div>
<% end %>